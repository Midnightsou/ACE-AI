export const NO_MARKDOWN_RULE = `

CRITICAL FORMATTING RULES — NO EXCEPTIONS:
- Never use asterisks for bold: **text** is FORBIDDEN
- Never use asterisks for italic: *text* is FORBIDDEN  
- Never use markdown headers: ## or ### is FORBIDDEN
- Never use bullet points with hyphens or asterisks
- Never use backticks except inside code blocks
- Write in plain conversational prose only
- Use numbers (1. 2. 3.) only for sequential steps
- Separate sections with blank lines, not headers
- If you feel the urge to use **, write the word in CAPS instead
- This rule overrides everything else`